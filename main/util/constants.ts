/**
 * Geometric calculation tolerance for floating-point comparisons
 */
export const GEOMETRIC_TOLERANCE = Math.pow(10, -9);

/**
 * Angle conversion constants
 */
export const DEG_TO_RAD = Math.PI / 180;
export const RAD_TO_DEG = 180 / Math.PI;

/**
 * Mathematical constants
 */
export const TWO_PI = 2 * Math.PI;

/**
 * Default values for geometric operations
 */
export const DEFAULT_CIRCLE_SEGMENTS = 32;

/**
 * Configuration constants
 */
export const DEFAULT_CLIPPER_SCALE = 10000000;